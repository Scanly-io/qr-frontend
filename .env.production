# ====================================
# QR Frontend - Production Environment
# ====================================
# Used by docker-compose.phase1.yml for Dokploy deployment.
# These are BUILD-TIME variables — baked into the Vite bundle.
# ====================================

# Backend API URL (your backend nginx gateway)
VITE_API_URL=http://api-scanwyse.duckdns.org

# Frontend URL (this frontend's public domain)
VITE_FRONTEND_URL=http://scanwyse.duckdns.org
